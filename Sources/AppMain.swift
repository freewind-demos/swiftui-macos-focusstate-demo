import SwiftUI

@main
struct FocusStateApp: App {
    var body: some Scene {
        Window("FocusState", id: "main") {
            ContentView()
        }
        .defaultSize(width: 400, height: 200)
    }
}